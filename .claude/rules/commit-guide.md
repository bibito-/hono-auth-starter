# コミット規約

コミットの作り方に関するルール。メイン Claude・各種 agent の双方に適用される。

## コミット粒度

修正・実装は意味のある単位ごとに独立してコミットする。複数の修正指摘・変更点をまとめて1コミットにしない。

レビュー指摘が複数ある場合も、指摘ごとに個別コミットを作る。

## git add のスコープ

`git add -A` / `git add .` は使わない。コミット対象のファイルを明示的に列挙すること。

意図しないファイル（作業中の別変更・ローカル設定・一時ファイル）が混入するのを防ぐため。
